{
  "name": "cumbuco-net-br",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "test": "node --test worker/index.test.js",
    "audit:seo": "npm run build && node scripts/audit-seo.mjs",
    "audit:properties": "node scripts/audit-property-facts.mjs",
    "audit:live": "node scripts/audit-live.mjs",
    "report:funnel": "node scripts/report-funnel.mjs",
    "content:prepare": "node scripts/prepare-content.mjs",
    "images:optimize": "node scripts/optimize-images.mjs",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy",
    "migrate:export": "node scripts/export-legacy-br.mjs",
    "migrate:images": "node scripts/download-legacy-images-br.mjs",
    "migrate:reconcile": "node scripts/reconcile-properties-br.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "7.2.0"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "wrangler": "4.120.1"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
